$(".search-input").keyup(() => {
  console.log("_in user: " + $(".search-input").val());
});
$(".nav-sidebar").removeClass("active");
$(".user-nav-sidebar").addClass("active");

// AJAX

$("body").on("click", ".page-item", function () {
  $.ajax;
});
